{
  "name": "finance-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "wrangler dev --assets dist"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "wrangler": "^3.80.0"
  }
}
